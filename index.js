import React from 'react';
import ReactDOM from 'react-dom';
import RandomCountdownApp from './random-countdown-app';

ReactDOM.render(
  <React.StrictMode>
    <RandomCountdownApp />
  </React.StrictMode>,
  document.getElementById('root')
);
